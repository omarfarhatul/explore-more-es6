/* 
1. var let const
2. default patameter
3. templete string
4. arrow function
5. destructuring and spread operator
6. Object.keys, Object.values, Object.entries
7. for of used in array and string
8. for in loop used in object
*/

const a=34;
const numbers=[35, 23, 32];
const person = {
    name: 'Sakib Khan'
}
const message=`Hi, ${person.name} has a: ${a} access to ${numbers[2]}`

if(true){

}

const square = x => x*x;
const sum = (a, b) => a+b;

const {}={x: 2, y: 5, z: 6, name: 'ovijeet', age: 55}
const [first, second, last] =['sam', 'jam', 'kim']